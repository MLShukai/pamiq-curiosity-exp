# @package interaction.agent.curiosity

_target_: exp.agents.curiosity.delta_minimize.DeltaMinimizeAgent
max_imagination_steps: ${shared.max_imagination_steps}

device: ${shared.device}
dtype: ${shared.dtype}
