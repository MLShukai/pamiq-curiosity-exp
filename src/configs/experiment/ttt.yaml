# @package _global_

defaults:
  - override /interaction/agent: ttt
  - override /trainers: ttt
  - override /models: ttt
  - override /buffers: ttt

shared:
  dtype: float32

experiment_name: "ttt"

tags:
  - "ttt"
  - "adversarial"
